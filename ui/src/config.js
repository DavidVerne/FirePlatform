const config = {
  // basename: '/dashboard',
  // defaultPath: '/dashboard/default',
  // fontFamily: `'Roboto', sans-serif`,
  // borderRadius: 12
  basename: '/',
  defaultPath: '/landing',
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 12
};

export default config;
